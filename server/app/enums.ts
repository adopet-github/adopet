export enum HouseTypes {
  VILLA = 'villa',
  APARTMENT = 'apartment',
  HOUSE = 'house',
  TOWNHOUSE = 'townhouse'
};

export enum InputTypes {
  PARAMS = 'params',
  BODY = 'body',
  QUERY = 'query'
};

export enum AccountTypes {
  ADMIN = 'admin',
  SHELTER = 'shelter',
  ADOPTER = 'adopter'
}